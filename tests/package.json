{
  "name": "genroc-tests",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "generate": "openapi-typescript ../openapi.json -o generated/api.ts",
    "playground:generate": "bun run playground/codegen.ts",
    "playground:server": "bun run playground/server.ts",
    "playground:run": "bun run playground/run.ts",
    "bench-recursive": "bun run bench/run.ts recursive",
    "bench-deep": "bun run bench/run.ts deep",
    "bench-drain": "bun run bench/run.ts drain",
    "bench-drain-big": "bun run bench/run.ts drain_big"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "openapi-fetch": "^0.17.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^25.9.0",
    "json-schema-to-typescript": "^15.0.4",
    "openapi-typescript": "^7.13.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
