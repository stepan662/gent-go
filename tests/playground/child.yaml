# yaml-language-server: $schema=http://localhost:8888/process-schema.json
name: child
steps:
  - id: child
    call:
      type: rest
      endpoint: http://localhost:3001/child
      output_schema:
        type: object
        properties:
          ok:
            type: boolean
        required: [ok]
